terraform {
  backend "azurerm" {
    resource_group_name  = "StorageAccount-ResourceGroup"
    storage_account_name = "p2agentinfraterraform"
    container_name       = ""
    key                  = "terraform.tfstate"
  }
}
